Version 1.0.0 features:
1. 自动将依赖jar加入特定的目录下
2. manifest增加Class-Path
3. 负责直接部署(拷贝)到特定目录，对于开发EJB jar时很有用

Version 1.1.0 features:
1. manifest增加Main-Class
2. 加入boot-jar支持，自动使得打出的jar包可以直接run